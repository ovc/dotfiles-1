#!/usr/bin/env bash
# vi-which: wrapper for the classic"vi $(which script)".

prog="$1"
type "$prog" &>/dev/null
if [ "$?" -ne 0 ]; then
	echo "\"${prog}\" not found in \$PATH." >&2
	exit 1
else 
	vi $(which "$1")
	exit 0
fi

