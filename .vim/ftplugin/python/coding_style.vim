" Follow the PEP 8 convention.
setlocal expandtab			" Indent with spaces.
setlocal tabstop=4			" Let a tab be 4 spaces wide.
setlocal shiftwidth=4			" Tab width for auto indent and >> shifting.
setlocal softtabstop=4			" Number of spaces to act on for editing actions like BS and tab.
setlocal smarttab			" Indent with spaces at beginning of line.
setlocal shiftround			" Round indentation to nearest multiple of shiftwidth with <,>.
